const { User } = require('../models');


const resolvers = {
    Query: {

    },
    Mutation: {
      addUser: async () => {
  
      },
      login: async () => {
  
      }
    }
  };
  
  module.exports = resolvers;